import React from 'react';
import './styles.css';

const cName = 'WheelSelector';

const WheelSelector = (props) => (
  <div className={cName}>
    Wheel Selector
  </div>
);

export default WheelSelector;
